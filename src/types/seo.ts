export enum TwitterCards {
  summary = 'summary',
  summaryLargeImage = 'summary_large_image',
  app = 'app',
  player = 'player'
}

export enum OGTypes {
  article = 'article',
  book = 'book',
  profile = 'profile',
  website = 'website'
}

export enum TwitterTag {
  booksquare = '@booksqaure_se'
}

export enum StructuredType {
  Website = 'Website',
  Product = 'Product'
}
