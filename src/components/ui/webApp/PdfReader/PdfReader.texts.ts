import { defineMessages } from 'react-intl';

export const texts = defineMessages({
  reloadText: {
    id: 'PdfReader.reloadText',
    description: 'Label for reload button',
    defaultMessage: 'Ladda om bok'
  }
});
