import { defineMessages } from 'react-intl';

export const texts = defineMessages({
  authorLabel: {
    id: 'HeadlineSection.authorLabel',
    description: 'Label for authors',
    defaultMessage: 'Författare: '
  }
});
