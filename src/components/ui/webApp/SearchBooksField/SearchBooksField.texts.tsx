import { defineMessages } from 'react-intl';

export const texts = defineMessages({
  emptyValidation: {
    id: 'SearchBooksField.emptyValidation',
    description: 'Validation message when search field is empty',
    defaultMessage: 'Obligatorisk'
  }
});
