import { defineMessages } from 'react-intl';

export const texts = defineMessages({
  error: {
    id: 'Reader.error',
    description: 'Reader error',
    defaultMessage: 'Något gick fel vid hämtningen, vi felsöker'
  }
});
